"use client";
import Home from "./components/Home/Home";

/**
 * Page d'accueil — affiche uniquement le formulaire de connexion.
 * Le Provider Redux est géré par ClientLayout (parent).
 */
export default function Page() {
  return <Home />;
}
